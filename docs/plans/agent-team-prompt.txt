Read the audit plan at docs/plans/2026-03-03-admin-portal-audit.md — it contains 16 prioritized issues for the admin portal.

Create an agent team with 3 teammates to fix these issues in parallel. Use Sonnet for each teammate. Require plan approval before they make any changes.

Teammate assignments (each teammate owns DIFFERENT files to avoid conflicts):

TEAMMATE 1 — "api-fixer" — Fixes backend API routes:
- PROD-1: Remove full-collection scan from src/app/api/admin/stats/route.tsx (remove fallback at lines 84-100, fix unique users query at lines 102-112). Delete the cachedStats logic too (ARCH-4).
- PROD-2: Add .limit(50) to src/app/api/admin/escalations/route.ts query at line 49
- PROD-4: Fix z.string().uuid() validation in src/app/api/admin/escalations/resolve/route.ts line 13 — change to z.string().min(1).max(128). Also check if src/app/(main)/api/chat/history/route.ts line 56 and src/app/(main)/api/chat/escalate/route.ts line 18 need the same fix (verify if sessionIds in Firestore are UUIDs or auto-IDs first).
- QUALITY-3: Rename src/app/api/admin/stats/route.tsx to route.ts (no JSX in the file)

TEAMMATE 2 — "frontend-fixer" — Fixes React components:
- BUG-1: Add try/catch to handleContactStatusChange in src/app/(main)/admin/requests/page.tsx — wrap the updateContactStatus call, only update local state on success, show console.error on failure
- BUG-2: Escape newlines in CSV export in src/app/(main)/admin/chats/page.tsx — add .replace(/\n/g, ' ').replace(/\r/g, '') after the existing .replace(/"/g, '""')
- QUALITY-5: Extract the formatTime() function (duplicated in admin/page.tsx:75, admin/chats/page.tsx:219, admin/requests/page.tsx:42) to a new file src/utils/format.ts, then update all 3 pages to import from it
- MISSING-5: Create src/app/(main)/admin/error.tsx as a React error boundary with admin-themed UI using the existing CSS variable system (var(--theme-*))

TEAMMATE 3 — "reviewer" — Read-only validation (do NOT edit any files):
- Wait for teammates 1 and 2 to finish their tasks
- Run npx tsc --noEmit to check TypeScript compiles with zero errors
- Review every file changed by teammates 1 and 2 against the audit plan
- Verify no regressions were introduced (auth checks still present, rate limiting intact, Greek text unchanged)
- Report pass/fail for each fix with specific reasoning

IMPORTANT RULES FOR ALL TEAMMATES:
- This is a Next.js 16.1 + Firebase + Tailwind CSS project hosted on Vercel (serverless)
- Keep ALL Greek (el-GR) UI text exactly as-is — do NOT translate anything
- Use existing code patterns and conventions from the codebase
- Each teammate ONLY touches their assigned files — no overlap
- Preserve the CSS variable theming system (var(--theme-text), var(--theme-accent), etc.)
- All API routes must maintain their existing rate limiting, auth checks, and error handling

Wait for all teammates to complete their tasks before proceeding.
