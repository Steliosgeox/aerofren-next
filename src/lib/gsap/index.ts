"use client";

/**
 * GSAP Animation System
 * 
 * Single source of truth for all GSAP animations.
 * Import from here, not from individual files.
 * 
 * @example
 * import { gsap, useGSAP, revealUp, staggerReveal } from "@/lib/gsap";
 */

// Re-export client
export * from "./client";

// Re-export presets
export * from "./presets";
